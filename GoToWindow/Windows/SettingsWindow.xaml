<Window x:Class="GoToWindow.Windows.SettingsWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:GoToWindow.ViewModels"
	xmlns:converters="clr-namespace:GoToWindow.Converters"
	mc:Ignorable="d"
    Title="Go To Window Settings" 
	Icon="/GoToWindow;component/Resources/AppIcon.ico"
	WindowStartupLocation="CenterScreen"
	WindowStyle="ToolWindow"
	SizeToContent="Manual"
	Width="720" Height="350"
	MinWidth="500" MinHeight="250"
	Initialized="Window_Initialized"
    d:DataContext="{d:DesignInstance viewModels:DesignTimeSettingsViewModel, IsDesignTimeCreatable=True}">
	<Window.Resources>
		<converters:IncrementIntConverter x:Key="IncrementIntConverter" />
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Name="TabsRow" Height="*" />
			<RowDefinition Name="ButtonsRow" Height="Auto" />
		</Grid.RowDefinitions>
		<TabControl Grid.Row="0" Margin="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

			<TabItem Header="_About">
				<Grid Margin="12">
					<StackPanel Orientation="Vertical">
						<StackPanel.Resources>
							<Style TargetType="{x:Type TextBlock}">
								<Setter Property="Margin" Value="0,0,0,6"/>
							</Style>
						</StackPanel.Resources>
						<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top">
							<Span>GoToWindow v</Span>
							<Run Text="{Binding Version}" FontWeight="Bold" />
						</TextBlock>
						<TextBlock>           
							<Span>For more information and to download the latest version, go to </Span>
							<Hyperlink NavigateUri="http://www.google.com" RequestNavigate="Hyperlink_RequestNavigate">http://christianrondeau.github.io/GoToWindow</Hyperlink>
						</TextBlock>
						<TextBlock Text="© Christian Rondeau 2014" FontStyle="Italic" />
						<TextBlock x:Name="NoElevatedPrivilegesWarning" Visibility="{Binding NoElevatedPrivilegesWarning}" Foreground="Red" TextWrapping="Wrap">
							<Span FontWeight="Bold">Go To Window is currently not running with administrative privileges (Run as Administrator).</Span>
							<LineBreak />
							<Span>Alt + Tab will not work when the current window runs with elevated privileges.</Span>
							<LineBreak />
							<Span>Some performance improvements will also be disabled.</Span>
							<LineBreak />
							<Span>Restart GoToWindow as an Administrator to remove this notice.</Span>
						</TextBlock>
					</StackPanel>
				</Grid>
			</TabItem>

			<TabItem Header="_Settings">
				<Grid>
					<CheckBox x:Name="HookAltTabCheckbox" Content="_Enabled" IsChecked="{Binding HookAltTab}" HorizontalAlignment="Left" Margin="255,19,0,0" VerticalAlignment="Top" />
					<Label Content="Shortcut" Width="Auto" Height="Auto" HorizontalAlignment="Left" Margin="10,12,0,0" VerticalAlignment="Top"/>
					<ComboBox Width="180" x:Name="ShortcutPressesBeforeOpenComboBox" IsEnabled="{Binding HookAltTab}" SelectedIndex="{Binding ShortcutPressesBeforeOpen, Converter={StaticResource IncrementIntConverter}, ConverterParameter=-1}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="70,16,0,0">
						<ComboBoxItem Tag="1">Single (Alt + Tab)</ComboBoxItem>
						<ComboBoxItem Tag="2">Twice (Alt + Tab + Tab)</ComboBoxItem>
					</ComboBox>
					<CheckBox x:Name="StartWithWindowsCheckbox" Content="_Start with Windows" IsChecked="{Binding StartWithWindows}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,43,0,0"/>
					<CheckBox x:Name="WindowListSingleClick" Content="Click once to select an item in the list" IsChecked="{Binding WindowListSingleClick}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,63,0,0"/>
				</Grid>
			</TabItem>

			<TabItem Header="_Plugins">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<StackPanel Grid.Row="0" Orientation="Vertical" Margin="8">
						<TextBlock FontWeight="Bold">Uncheck a plug-in to disable it.</TextBlock>
						<TextBlock>You can uninstall a plug-in completely by removing it from the Plugins directory.</TextBlock>
					</StackPanel>
					<ListView Grid.Row="1" ItemsSource="{Binding Plugins}">
						<ListView.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<CheckBox IsChecked="{Binding Enabled}" Content="{Binding Name}" />
								</StackPanel>
							</DataTemplate>
						</ListView.ItemTemplate>
					</ListView>
				</Grid>
			</TabItem>

			<TabItem Header="_Help">
				<WebBrowser Name="HelpWebBrowser" />
			</TabItem>

		</TabControl>
		<Grid Grid.Row="1" Margin="8,0,8,8" HorizontalAlignment="Stretch">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
				<StackPanel.Resources>
					<Style TargetType="{x:Type Button}">
						<Setter Property="Margin" Value="8,0,0,0"/>
					</Style>
				</StackPanel.Resources>
				<Button x:Name="OkButton" IsDefault="True" Content="Apply" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75" Click="OkButton_Click"/>
				<Button x:Name="CancelButton" IsCancel="True"  Content="Close" HorizontalAlignment="Left" VerticalAlignment="Center" Width="75" Click="CancelButton_Click"/>
			</StackPanel>
		</Grid>
	</Grid>
</Window>
